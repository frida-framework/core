# ─── LAYER 1: IDENTITY & META ────────────────────────────────────────────────

CANON_META:
  _visibility: private
  id: frida-canon-meta
  version: 2.0.0
  owner: frida-core
  updated_at: "2026-02-19"
  scope:
    repository: hanszel/frida
    package: "@hanszel/core"
    purpose: >-
      Schema-driven generation engine, zone enforcement, visual overlay extraction,
      agent session control, and project scaffold for Frida-managed repositories.
  source_ref: FRIDA_CORE_CANON

CANON_UNIQUENESS:
  _visibility: private
  id: frida-canon-uniqueness-policy
  version: 1.0.0
  first_rule: true
  rule: "Any canon entity MUST be declared exactly once."
  control:
    enforcement: warn_only
    fail_on_duplicate: false
    warning_code: CANON_DUPLICATE_ENTITY
  strictness_freeze:
    change_requires_explicit_owner_instruction: true
    note: "Strictness level changes require explicit owner approval."
  projection_rule:
    allow_projection_via_source_ref: true
    note: "Blocks may reference other blocks via source_ref without duplication."

meta:
  id: frida-meta
  version: 1.0.0
  schemaVersion: "2.0"
  mode: schema
  canonArtifactRef: "PATHS.contract.coreCanonFile"

core:
  id: frida-assembly-core
  version: 1.0.0
  canonicalSourceBlocks:
    - CANON_META
    - CANON_UNIQUENESS
    - FRIDA_SCHEMA_SUPPORT
    - FRIDA_ARCHITECTURE_DECISIONS
    - FRIDA_FOUNDATION_PRINCIPLES
    - PATHS
    - FRIDA_CONFIG_SPEC
    - FRIDA_ASSEMBLY_PROTOCOL
    - FRIDA_REFS
    - FRIDA_CONFIG
    - FRIDA_RUNTIME_CONFIG
    - ZONES
    - FRIDA_TASK_PROFILES
    - FRIDA_ENFORCEMENT
    - FRIDA_AGENT_LIFECYCLE
    - FRIDA_ENFORCEMENT_TYPES
    - FRIDA_REPORTING
    - FRIDA_VALIDATION_CHECKLIST
    - FRIDA_TEMPLATE_INTEGRITY
    - FRIDA_CLI
    - FRIDA_VISUAL
    - FRIDA_GUARD_SPEC
    - FRIDA_INVARIANT_SPEC
    - FRIDA_ANTITASK_SPEC
    - FRIDA_INTERFACE_VERIFY
    - FRIDA_INTERFACE_UPDATE_SPEC
    - FRIDA_INTERFACE_UPDATE_CODE
    - FRIDA_INTERFACE_TASKSET
    - FRIDA_SCAFFOLD
  selectorGrammar: jsonpath-lite
  generatorPipelineContracts:
    - "schema-validate"
    - "normalize"
    - "resolve-selectors"
    - "run-legacy-generator"
    - "run-adapter-generators"
    - "emit-manifest"
  pathRefs:
    canonArtifact: "contract/core.canon.yaml"
    templatesFrida: "templates/frida"
    templatesDocs: "templates/docs-gen"
  outputContracts:
    - ".frida/profiles/*.xml"
    - ".frida/AGENTS.md"
    - ".frida/docs/policy/*.md"
    - ".frida/docs/reference/*.md"
    - ".frida/config.yaml"
    - ".frida/frida.adapter.generated.json"
