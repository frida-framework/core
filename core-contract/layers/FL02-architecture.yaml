# ─── FRIDA LAYER FL02: ARCHITECTURE ──────────────────────────────────────────

FRIDA_SCHEMA_SUPPORT:
  _visibility: private
  id: frida-schema-support
  version: 1.0.0
  schema_version:
    supported: 1.0.0
  path_rules:
    - id: no_leading_slash
      text: "path MUST NOT start with '/'."
    - id: no_backslash
      text: "path MUST NOT contain '\\'."
    - id: no_parent_traversal
      text: "path MUST NOT contain '..'."
  guards:
    - id: invalid_layer_path
      trigger: invalid_layer_path
      action: HALT
      error_code: INVALID_LAYER_PATH

FRIDA_ARCHITECTURE_DECISIONS:
  _visibility: private
  id: frida-architecture-decisions
  version: 1.0.0
  decisions:
    - id: CORE-ADR-001
      title: Schema-first contract is the primary normalization axis
      status: ACCEPTED
      decision:
        - "Generation input MUST be schema-native contract with top-level meta + core blocks."
        - "Contract model MUST be validated against schemas/frida-contract.schema.json."
        - "In strict mode, deprecated contract fields are treated as hard failure."
      evidence:
        - src/schema.ts
        - schemas/frida-contract.schema.json
        - src/runtime.ts

    - id: CORE-ADR-002
      title: Deterministic selectors for source binding
      status: ACCEPTED
      decision:
        - "All source data binding uses jsonpath-lite selectors."
        - "Required selectors MUST match at least one contract node."
        - "Selector resolution is stateless and deterministic."
      evidence:
        - src/selector.ts

    - id: CORE-ADR-004
      title: Extension via adapter pattern
      status: ACCEPTED
      decision:
        - "App-specific generation registers adapters implementing FridaAdapter."
        - "Adapters contribute selectors, generators, and guards."
        - "Core runtime orchestrates adapter execution via registry."

    - id: CORE-ADR-005
      title: Zone-driven routing replaces heuristic matching
      status: ACCEPTED
      decision:
        - "Zone resolution uses path-specificity algorithm, not profile heuristics."
        - "Each zone has a contractical AGENTS.md path derived from agentsPath."
        - "Zone resolution is deterministic: normalize → match → rank → select."
      evidence:
        - src/zone-check.ts

    - id: CORE-ADR-006
      title: Template integrity via hash manifest
      status: ACCEPTED
      decision:
        - "Template .hbs files are source-of-truth, not inline content."
        - "Drift detection uses SHA-256 manifest comparison."
        - "Hash mismatch returns non-zero exit code."
      evidence:
        - src/template-hash.ts
        - contract/template-integrity.manifest.yaml

    - id: CORE-ADR-007
      title: Reporting contract is fixed and immutable
      status: ACCEPTED
      decision:
        - "Reporting path, extension, and filename pattern are constants."
        - "Runtime config is generated from contract, not edited manually."
        - "Init command normalizes reporting settings to fixed values."
      evidence:
        - src/reporting-contract.ts
        - src/init.ts

    - id: CORE-ADR-008
      title: Visual overlay uses boundary-first semantic zoom
      status: ACCEPTED
      decision:
        - "Component-level contract file is the primary visual projection unit."
        - "FRIDA_INTERFACE_SELF_CONTRACT_MANAGEMENT.component_contract_spec is the authoritative source for component-boundary source semantics."
        - "FRIDA_VISUAL.component_projection is the authoritative source for source-contract-to-overlay projection semantics."
        - "FRIDA_VISUAL.overlay_entity_model_v1 is the authoritative source for derived overlay entity semantics, and FRIDA_VISUAL.overlay_schema_v1 is authoritative for overlay serialization and artifact delivery."
        - "FRIDA_VISUAL.viewer_runtime_v1 is the authoritative runtime/viewer contract and MUST consume overlay, not raw contract, for visual semantics."
        - "Component boundary semantics are established before any YAML-depth expansion."
        - "Topology, flow, and specification are interpreted relative to the current component boundary."
        - "component_domain_blocks projection is selector-driven only through explicit projection_domains declarations on top-level child blocks; no shape, key-name, or UI heuristics are allowed."
        - "Mounted child internals stay collapsed in parent specification view until explicit drill-in through component_mount_point.mounted_child_boundaryRefs."
        - "Mounted-child, continuation, and return refs produce typed overlay relations before any generic edge projection."
        - "Reference-viewer demo state is fixture-only and MUST NOT be taught as general component_input_interface semantics."
        - "Overlay is deterministic: identical normalized contract input and identical generator build/configuration yield byte-identical overlay output after normalization of explicitly declared volatile fields only."
      evidence:
        - src/visual.ts

    - id: CORE-ADR-009
      title: Agent sessions enforce exclusive sequential access
      status: ACCEPTED
      decision:
        - "Agent acquires session token before mutation."
        - "Zone-scoped hard check rolls back changes outside target zone."
        - "Stale token triggers routing regeneration after timeout."
        - "Session ends with PR creation or explicit release."
      evidence:
        - "Future: src/session.ts"

    - id: CORE-ADR-010
      title: Warm-first bootstrap reconciles package-reference Frida surfaces
      status: ACCEPTED
      decision:
        - "frida-core bootstrap is a manual-only command; default mode is warm reconcile of Frida-managed surfaces."
        - "Package files and package manifest are the reference for bootstrap-managed surfaces under drift."
        - "Warm/cold-engine bootstrap MUST NOT modify app-contract files."
        - "Frida app-side generation/bootstrap reads app-contract only from .frida/inbox/app-contract/** and does not fall back to contract/**."
        - "Explicit demo mode is zero-deploy only and is the only mode that may write demo app-contract seed."
        - "Warm/cold-engine bootstrap runs generation after reconcile to restore engine-generated artifacts."
        - "Warm/cold-engine bootstrap post-generation MUST satisfy strict .frida root layout policy."
        - "Explicit component-reconcile mode remains idempotent and scoped."
      evidence:
        - "Future: src/bootstrap.ts"

    - id: CORE-ADR-011
      title: Contract interfaces bridge natural language and code
      status: ACCEPTED
      decision:
        - "Intent-to-contract interface converts user specifications to contract YAML."
        - "Code-to-contract interface extracts contract from existing code."
        - "Both produce draft contract requiring human or architect approval."
        - "Interface semantics are authoritative: required concepts, rules, invariants, guards, verification, and reporting expectations live in the interface block."
        - "Playbooks operationalize interfaces as derived instructions and may be adapted in form when they are public target-repo interface-instruction surfaces."
        - "AGENTS.md and other engine entry/routing surfaces remain strict engine invariants and are not user-adaptable forms."
      evidence:
        - "Future: src/contract-interfaces.ts"

FRIDA_FOUNDATION_PRINCIPLES:
  _visibility: private
  id: frida-frida-foundation-principles
  version: 1.0.0
  development_sequence:
    order:
      - CONTRACT
      - ANTITASK
      - DEVELOPMENT
    rule: "No development step begins without a contract. No contract is valid without anti-task analysis."
  principles:
    - id: schema_first
      text: "Schema-first normalization axis is mandatory."
    - id: strict_path_refs
      text: "All paths outside the PATHS block MUST be references (*Ref). Direct paths are forbidden."
    - id: contract_source_allowlist
      text: "Contract source is limited to declared contractical files. No runtime inference."
    - id: no_heuristics
      text: "No heuristics for contract interpretation; explicit contract data only."
    - id: fail_fast
      text: "Fail-fast when mandatory contract or surface is missing."
    - id: reproducibility
      text: "Reproducibility and determinism are mandatory gates."
    - id: single_source_of_truth
      text: "Each datum has exactly one contractical source. Overlays are derived, never authoritative."
    - id: context_minimization
      text: "Agent context is minimal and contract-scoped. No ambient knowledge."
    - id: architect_visual_loop
      text: "Visual artifacts support the architect loop: see → model → commit."
  migration_rule:
    default: "Adopt stable old-kernel principles unless conflict is detected."
    conflict_resolution: "Current core contract has priority and is treated as refinement."
    wiki_only_policy: "Wiki-only postulates from old kernel are deprecated in package core."
  contract_design_principles:
    - id: engine_app_separation
      text: "Engine describes universal mechanisms. App describes project-specific data. No cross-contamination."
    - id: brief_engine_detail_app
      text: "Engine defines WHAT a mechanism does. App defines HOW it is configured."
    - id: layer_single_purpose
      text: "Each layer has one clear purpose. Block placement follows purpose, not convenience."
    - id: config_details_live_in_config_layer
      text: >-
        Concrete engine settings and configurable literals MUST live in the
        core-configuration layer file. Other layers may reference config
        entries only and MUST NOT duplicate config details inline.
    - id: frida_root_ownership_split
      text: "`.frida` root is an ownership-split surface: only runtime config files at root; engine-managed artifacts in `.frida/contract`, reports in `.frida/reports`, and inbound SSOT data in profile-gated `.frida/inbox`, where `.frida/inbox/app-contract/**` is writable only through app-contract update interfaces."
    - id: name_reflects_content
      text: "Names must accurately describe content. Misleading names must be corrected."
    - id: domain_grouped_validation
      text: "Guards and invariants are grouped by domain for structured auditing."
    - id: buildtime_management_cli_separation
      text: "Build-time mechanisms, contract interaction interfaces, and CLI are separate domains."
    - id: no_loss_on_transform
      text: "Every structural transformation is audited. Lost data is restored or explicitly deprecated."
    - id: defaults_are_noise
      text: "Default values are not stated. Explicit value means conscious choice."
    - id: build_after_every_change
      text: "Build must pass after every contract change."
    - id: contract_is_governance
      text: "Contract is governance data. No runtime state, no mutable data."
    - id: interfaces_live_in_management
      text: "Contract interfaces are defined by management-layer blocks; interface meaning MUST NOT be inferred from unrelated layers or prose."
    - id: interfaces_default_public
      text: "FRIDA_INTERFACE_* blocks are public unless `_visibility: private` is explicitly declared."
    - id: private_interfaces_not_deployed
      text: "Private interfaces are internal to Frida core and MUST NOT be projected into target-repo Frida mirrors."
    - id: self_contract_management_repo_only
      text: "FRIDA_INTERFACE_SELF_CONTRACT_MANAGEMENT is repo-local to `frida` and is the only interface allowed to update the Frida contract itself."
    - id: app_contract_update_interfaces_repo_only
      text: "FRIDA_INTERFACE_UPDATE_APP_BY_SPEC and FRIDA_INTERFACE_UPDATE_APP_BY_CODE apply only in target app repos and may mutate only the target app contract."
    - id: frida_tasks_core_only
      text: "`core-tasks/` is a Frida-core-only task surface. `tasks/` is forbidden in the Frida repo, and `core-tasks/` is forbidden outside the Frida repo."
    - id: zones_public_external_internal_private
      text: "`ZONES` names routing entities outside repo `frida`; private Frida-repo routing entities live only in `INT_FRIDA_ZONES` and MUST NOT be projected into target repos."
    - id: interface_declaration_only
      text: >-
        Contract interfaces are authoritative for semantics and instruction
        requirements. Derived playbooks operationalize those semantics, but
        playbook wording and structure remain adaptable for public
        interface-instruction surfaces unless the surface is explicitly marked
        as engine-invariant.
    - id: playbooks_derived_not_authoritative
      text: "Playbooks and other interface-instruction documents are derived operational surfaces. They MUST cite their backing interface and MUST NOT become independent sources of normative meaning."
    - id: interface_instruction_form_adaptable
      text: "Public interface-instruction surfaces may vary in wording, order, and layout so long as the interface-mandated normative elements remain present."
    - id: engine_instruction_surfaces_strict
      text: "Engine entry/routing instruction surfaces such as AGENTS.md, ROUTER.xml, profile XMLs, and contractual zone AGENTS.md remain exact engine invariants and may be regenerated or overwritten by Frida."
    - id: component_context_fit
      text: >-
        Every component contract file MUST fit entirely in the agent context window.
        Component contract is a self-contained source for task formulation.
        It is the primary visual projection unit for its current component boundary.
        It declares: component_hierarchy_position, component_mount_point,
        component_input_interface, component_output_interface,
        component_domain_blocks, and component_shared_refs. These names define
        component-boundary semantics, not repo/file/global hierarchy,
        filesystem mount semantics, or arbitrary runtime props/events outside
        the contract surface. Mounted child internals are reached only by
        explicit boundary-aware drill-in.

